[build-system]
requires = ["setuptools>=83.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pixshift"
version = "1.1.0"
description = "Fast, safe, automation-first image and PDF CLI toolkit"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    {name = "PixShift Team"},
]
keywords = ["image", "pdf", "converter", "automation", "batch", "cli"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Intended Audience :: End Users/Desktop",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Multimedia :: Graphics :: Graphics Conversion",
]

dependencies = [
    "Pillow>=9.0",
    "pillow-heif>=0.10.0",
    "click>=8.0",
    "rich>=12.0",
    "PyMuPDF>=1.23.0",
]

[project.optional-dependencies]
avif = ["pillow-avif-plugin"]
all = ["pillow-avif-plugin"]
dev = [
    "build>=1.2.0",
    "hypothesis>=6.165.0",
    "mypy>=1.10",
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "ruff>=0.8",
    "twine>=5.0.0",
]

[project.scripts]
pixshift = "pixshift.cli:main"

[project.urls]
Homepage = "https://github.com/ChangWinde/pixshift"
Repository = "https://github.com/ChangWinde/pixshift"
Issues = "https://github.com/ChangWinde/pixshift/issues"
Documentation = "https://github.com/ChangWinde/pixshift#readme"

[tool.setuptools.packages.find]
include = ["pixshift*"]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.10"
ignore_missing_imports = true
check_untyped_defs = true
disallow_untyped_defs = true
exclude = ["^tests/"]
